<%
  @title = "Places"
  @back_url = menu_path
  @extra_header = link_to "Add", new_place_path, { class: "btn btn-primary btn-block btn-sm", data: { test: 'add' } }
%>

<input class="form-control" id="placeSearch">

<table class="table">
  <tbody>
  <% @places.each do |place| %>
    <%= render "places/index_row", place: place %>
  <% end %>
  </tbody>
</table>

<script>
  $(() => {
      $('#placeSearch').keyup(() => {
      })
  })
</script>
